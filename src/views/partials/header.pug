header
    nav
        a(href="/").header__logo
            i.fab.fa-youtube
        ul
            form(action="/search", method="GET").serach__form
                input(placeholder="Search by title",name="keyword" type="text").search__input
                button
                    i.fas.fa-search
            if loggedIn
                li 
                    a(href="/videos/upload") Upload Video
                li
                    a(href="/users/edit") Edit Profile
                li 
                    a(href="/users/logout") Logout
                li 
                    a(href=`/users/${loggedInUser._id}`)
                        if loggedInUser.avatarUrl === ""
                            span 😀
                        else
                            if loggedInUser.avatarUrl
                                if isHeroku
                                    img.avatar__small(src=loggedInUser.avatarUrl,crossorigin)
                                else 
                                    img.avatar__small(src="/" + loggedInUser.avatarUrl)
            else
                li 
                    a(href="/login") LogIn
                li 
                    a(href="/join").header__btn Join